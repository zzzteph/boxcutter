# Dependencies for `boxcutter serve` (the web UI/API server). NOT needed for the engine or `boxcutter agent`.
# In the Docker image these install into a dedicated venv (/opt/srv) so the lean engine imports stay untouched.
fastapi>=0.110
uvicorn[standard]>=0.27
sqlmodel>=0.0.16
pydantic-settings>=2.2
passlib[bcrypt]>=1.7
bcrypt>=4.0.1,<4.1
pyjwt>=2.8
python-multipart>=0.0.9
pymysql>=1.1
psycopg[binary]>=3.1     # Postgres driver — set DATABASE_URL=postgresql+psycopg://user:pass@host/db to use it
requests>=2.31
