# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Claude Code
.claude/

# Local scan output / scratch
runs/
runs2/
*.log

# Frontend build artifacts (the Docker image builds its own via the node stage)
web/node_modules/
web/dist/
web/.vite/

# Server runtime data (SQLite DB + generated secret; not source)
server/data/

# caleb coverage working area (harness, ground-truth keys, handoff docs, run artifacts) - kept on disk, not tracked
opt/

# Test target runtime DB + request logs + uploads + generated fixtures
test/store.db
test/logs/
test/uploads/
test/keys/
test/locales/
