# boxcutter's core dependency.
requests>=2.28

# The browser tools (harvest/login/actions) drive chromium over CDP with this. Pure-Python, installs
# on musl. Optional: when it's absent those tools degrade gracefully, the rest of boxcutter is unaffected.
websocket-client>=1.6
