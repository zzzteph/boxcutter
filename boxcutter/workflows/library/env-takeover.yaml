name: env-takeover
help: subfinder + dns-brute, then nuclei takeover templates on every discovered subdomain
input: target
output: findings

steps:
  - tool: subfinder
    target: ${target}
    save: hosts
  - tool: dns-brute
    target: ${target}
    save: hosts

  - for_each: ${hosts}
    do:
      - tool: nuclei
        target: ${hosts.item}
        args: '--opt-args="-tags takeover"'
        save: findings